{
  "name": "@izlearn/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Zod validation schemas shared between the izLearn frontend and backend.",
  "main": "schemas/index.ts",
  "types": "schemas/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
